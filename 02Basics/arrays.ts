const superHeroes: string[] = [];
// const heroPowers: number[] = [];
const heroPowers: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [
  [255, 255, 255],
  [1, 1, 1],
];

superHeroes.push("spiderman");
heroPowers.push(8);

allUsers.push({ name: "durpin", isActive: true });
